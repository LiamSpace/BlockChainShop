import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

export default new Router({
    routes:[
        // {
            //添加路由
            // path:'',
            // name:'login',
            // component: () => import('@/')
        // }
    ]
})